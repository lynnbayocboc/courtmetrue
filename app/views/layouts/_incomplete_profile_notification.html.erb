<% if !current_user.profile.finish_basic_info? || !current_user.profile.finish_personal_info? || !current_user.profile.finish_additional_info? || current_user.profile.has_atleast_1_pic? %>
  <div class="alert alert-danger">
    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
    Your profile is incomplete.
  </div>
<% end %>
