$("#favourite").html('<% if @is_favourite == 1 %>'+
                     '<%= link_to raw('<i class="glyphicon glyphicon-heart"></i>'), mark_as_favourite_path(@favourite.to_user_id,:is_favourite => false), title: "Unfavorite user",:remote=> true  %>'+
                     '<% else %>'+
                     '<%= link_to raw('<i class="glyphicon glyphicon-heart-empty"></i>'), mark_as_favourite_path(@favourite.to_user_id,:is_favourite => true), title: "Favorite user",:remote=> true  %>'+
                     '<% end %>')